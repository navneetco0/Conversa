import { Button, HStack, Text, VStack } from '@chakra-ui/react'
import React from 'react'
import { LinkButton } from './LinkButton'

export const SignUp = ({ setState }) => {
    return (
       <>
       </>
    )
}
